from utilities import argmax


def evaluate_policy(mdp, policy):
    return None


def extract_policy(mdp, values):


    policy = [
        # NOTE: in general, actions should be a function of the states
        #argmax(mdp.actions, lambda a: vlu ) .
        #for s in mdp.states
    ]


def generate_policy(mdp):



    return None
